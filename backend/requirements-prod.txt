# requirements-prod.txt — Extra packages needed for production (PostgreSQL).
# These are NOT needed for tests (tests use SQLite).
# Install on the production server with:
#   pip install -r requirements.txt -r requirements-prod.txt
psycopg2-binary==2.9.10
