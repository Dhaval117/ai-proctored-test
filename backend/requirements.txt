fastapi==0.115.5
uvicorn[standard]==0.32.1
pydantic[email]==2.10.3
email-validator==2.3.0
python-dotenv==1.0.1

# Database (Story 1.2+)
# Note: psycopg2-binary is listed in requirements-prod.txt (requires compiler on Windows).
# Tests use SQLite via DATABASE_URL override — no psycopg2 needed for CI.
sqlalchemy==2.0.36
alembic==1.14.0

# AI Orchestration
langchain>=0.3.0
langgraph>=0.2.0
langchain-google-genai>=2.0.0
